---
name: docker-web
description: Docker web application
kind: app
skeletonPath: ./skeleton
config:
  replicas: 2
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 8
    targetCPUUtilizationPercentage: 70
  resources:
    limits:
      cpu: 250m
      memory: 512Mi
